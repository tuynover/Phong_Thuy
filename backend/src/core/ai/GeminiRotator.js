const { AiRotator } = require('./AiRotator');

/**
 * GeminiRotator (Backward-compatibility Alias)
 * Trỏ trực tiếp tới AiRotator.gemini để duy trì tính tương thích ngược cho toàn bộ codebase hiện có.
 */
module.exports = AiRotator.gemini;
